# Copy to .env and fill in. .env is not committed.
# GEMINI_API_KEY or GOOGLE_API_KEY — either is enough.
GEMINI_API_KEY=
GOOGLE_API_KEY=
LOGFIRE_TOKEN=
# Optional. Reuse a running SurrealDB instead of starting one with brew.
SURREAL_URL=
